import { LienFilterPipe } from './lien-filter.pipe';

describe('LienFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LienFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
