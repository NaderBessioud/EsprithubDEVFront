import { CourseFilterPipePipe } from './course-filter-pipe.pipe';

describe('CourseFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
